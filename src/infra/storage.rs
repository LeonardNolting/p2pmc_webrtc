use bytes::Bytes;
use anyhow::Result;

// TODO do in dart for better UI
pub async fn get_server(id: String) -> Result<()> {
    Ok(())
}

// TODO do in dart for better UI
pub async fn get_jvm(id: String) -> Result<()> {
    Ok(())
}


// TODO do in dart for better UI
pub async fn push_server(id: String, server: Bytes) -> Result<()> {
    Ok(())
}


// TODO do in dart for better UI?
pub async fn run_server(id: String) -> Result<()> {
    // TODO run `/path/to/jre-21.0.6+7/Contents/Home/bin/java -Xmx1024M -Xms1024M -jar server.jar nogui`
    Ok(())
}

